import React from 'react'

export const LogoutSpotify = (props) => {
    return (
        <div>
            Logout text
        </div>
    )
}

LogoutSpotify.propTypes = {
    username: React.PropTypes.string
}
