import React, { PropTypes } from 'react'

export const HomePage = () => {

}

HomePage.propTypes = {
    fname: React.PropTypes.string,
    lanem: React.PropTypes.string,
    email: React.PropTypes.string,
}